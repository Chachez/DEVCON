import React from 'react';
import { TopBar, Sidebar } from './components';

const DefaultLayout = () => {
  return (
    <div>
      <TopBar />
      <Sidebar />
    </div>
  );
};

export default DefaultLayout;
